DB_USER=grit-admin
DB_DB_USER_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgresql
# Optional override (if set, this takes precedence over fields above)
# DATABASE_URL=postgresql+psycopg2://grit-admin:your_password@localhost:5432/postgresql
